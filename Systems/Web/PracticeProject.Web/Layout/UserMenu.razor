@using Microsoft.AspNetCore.Identity
@using PracticeProject.Context.Entities.Identity

@* @inject SignInManager<AuthorizedUsers> SignInManager
@inject UserManager<AuthorizedUsers> UserManager

@{
    var user = await UserManager.GetUserAsync(User);
    var fullName = user.FullName;
} *@

<MudMenu Color="Color.Inherit" Icon="@Icons.Material.Filled.Face" AnchorOrigin="Origin.BottomRight" >
    <MudMenuItem Link="/logout">Logout</MudMenuItem>
</MudMenu>